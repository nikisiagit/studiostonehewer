---
pagination:
  data: wp_projects
  size: 1
  alias: project
permalink: "projects/{{ project.slug | slugify }}/index.html"
layout: project.njk
eleventyComputed:
  title: "{{ project.title }}"
  seo_title: "{{ project.meta.title }}"
  seo_description: "{{ project.meta.description }}"
  seo_image: "{{ project.meta.image.url }}"
---
