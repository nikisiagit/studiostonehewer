---
layout: base.njk
---

<section class="projects-section reveal" style="padding-top: 150px; min-height: 80vh;">
  <div class="projects-header container" style="margin-bottom: 4rem; justify-content: center; text-align: center;">
    <div>
      <h4 class="section-subtitle">{{ subtitle }}</h4>
      <h2 class="section-title" style="margin-bottom: 1rem;">{{ header_title }}</h2>
      <p style="max-width: 600px; margin: 0 auto; color: var(--color-light-text);">{{ header_description }}</p>
    </div>
  </div>
  
  <div class="project-cards">
    {% for project in wp_projects %}
    <a href="/projects/{{ project.slug | slugify }}/" class="project-card">
      <div class="project-card-img-wrap">
        <span class="project-tag">{{ project.tag }}</span>
        <img src="{{ project.featured_image }}" alt="{{ project.title }}" class="project-card-img">
      </div>
      <div class="project-card-info">
        <h3 class="project-card-title">{{ project.title }}</h3>
        <p class="project-card-meta">{{ project.tag }} • {{ project.category }} • {{ project.year }}</p>
        <p class="project-card-desc">{{ project.description }}</p>
      </div>
    </a>
    {% endfor %}
  </div>
</section>
